INVISUAL — paket update #29 (Inbox: CSV + tandai penting  •  Alt-text galeri)

BAGIAN A — INBOX /admin/messages
- Tombol "Ekspor CSV": unduh semua pesan sebagai file .csv (buka di Excel/Sheets).
- Tombol bintang "Tandai penting" di tiap pesan + penanda bintang di judul.
- Filter "Penting saja" untuk menampilkan hanya pesan penting.

BAGIAN B — ALT-TEXT
- Gambar galeri proyek yang tadinya beralt kosong kini memakai judul karya
  sebagai teks alternatif (lebih baik untuk SEO Google & pembaca layar).
  (Gambar lain—logo, klien, tim, karya—sudah punya alt sebelumnya.)

LANGKAH 1 — Tambah kolom (jalankan SQL, sekali):
  Supabase -> SQL Editor -> tempel isi "messages-important.sql" -> Run.

LANGKAH 2 — Pasang kode (dari FOLDER ROOT proyek):
  unzip -o invisual-update-29.zip
  rm -rf .next && npm run dev
  (untuk live: commit & push ke GitHub)

- Tanpa npm install.
- Jalankan SQL (Langkah 1) DULU sebelum pasang kode, agar inbox tidak error.
